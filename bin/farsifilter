#!/bin/bash

PATH="/usr/local/bin:/bin:/usr/bin"
export PATH
# Find path
path="$0"
old_path=""
while test -n "$path"; do
  old_path="$path"
  path="`readlink "$path"`"
done
path="${old_path%/*}/.."

"$path/fcon/fcon" "$@"
